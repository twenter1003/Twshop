# 프로젝트: 한정 수량 판매 서비스 (B2C 백엔드 취업 포트폴리오)

## 목적
상용 서비스가 아니라 **취업 포트폴리오**다. 완성도보다 학습 밀도가 우선.
빠르게 동작하는 코드보다, 면접에서 왜 이렇게 만들었는지 설명 가능한 코드가 목표다.

## 스택 / 아키텍처
- Kotlin + Spring Boot, JPA + QueryDSL, MySQL, Redis
- Docker, GitHub Actions, AWS
- 모듈러 모놀리식 (패키지 단위 도메인 분리, 배포는 단일 앱)
- 모듈: User/Auth, Product/Inventory, Sale, Purchase, Waiting

## 행동 원칙 (반드시 지킬 것)

### 1. 설명 없이 구현하지 않는다
새로운 개념(락 전략, 인덱스 설계, 동시성 제어, 대기열 구조 등)이 처음 등장하는 작업이면
코드를 쓰기 전에 반드시:
1) 접근법 후보 2개 이상 제시
2) 각각의 트레이드오프 설명
3) 내가 하나를 고르거나 "네가 골라"라고 할 때까지 코드 작성 보류

이미 여러 번 다룬 반복 작업(단순 CRUD, 보일러플레이트)은 이 절차 생략 가능.

### 2. 한 번에 너무 많이 만들지 않는다
한 응답에서 여러 파일에 걸친 대규모 diff를 만들지 마라. 기능 하나, 테스트 하나 단위로 끊는다.
이전 프로젝트(Muster)가 스코프 과다로 폐기된 전례가 있다 — 같은 실수를 반복하지 않는다.

### 3. 테스트 없이 "완료"라고 하지 않는다
새 로직에는 테스트를 함께 작성한다. 테스트가 없으면 그 작업은 미완료로 간주한다.

### 4. 의사결정을 자동으로 기록한다
아키텍처/기술 선택이 있을 때마다 `/docs/decisions.md`에 다음 형식으로 append:
- 날짜, 결정 사항, 검토한 대안, 선택 이유, 감수한 단점
내가 "기록해"라고 말하지 않아도 알아서 남긴다.

### 5. 벤치마크 없이 "성능이 개선됐다"고 말하지 않는다
동시성/성능 관련 변경은 반드시 before/after 수치가 있어야 한다.
수치가 없으면 "측정이 필요하다"고 먼저 말해라.

### 6. Vibe coding 유도 금지
내가 코드를 읽지 않고 그냥 승인하도록 유도하지 마라.
diff가 크거나 새 개념이 들어가면 "이 부분은 직접 읽어보시길 권합니다"라고 먼저 말해라.

### 7. 주석은 상세하게, 단 "왜"를 우선한다
- 모든 public 클래스/함수에 KDoc을 작성한다 (역할, 파라미터, 예외/에러 상황 포함).
- 비즈니스 규칙, 동시성 처리, 트레이드오프가 담긴 코드는 반드시 "왜 이렇게 했는지"를 주석으로 남긴다.
  예: `// 낙관적 락 대신 Redis 분산락 사용: 재고 1개 상품에서 재시도 비용이 더 크다고 판단 (2026-XX-XX 결정, decisions.md 참고)`
- getter, 단순 매핑처럼 자명한 코드에는 주석을 억지로 채우지 않는다.
  주석은 밀도가 아니라 "면접관이 코드만 보고 의도를 이해할 수 있는가"로 판단한다.

### 8. 실제 쇼핑몰 서비스라고 가정하고 설계하되, 구현 범위는 그대로 지킨다
- 도메인 모델과 인터페이스 경계는 실제 커머스 서비스 수준의 확장 시나리오
  (다양한 상품 유형, 다양한 판매 정책, 다양한 결제 수단 등)를 고려해서 설계한다.
- 단, 이것은 **설계 차원**의 이야기다. 인터페이스/추상화 수준에서 확장 여지를 남기되,
  실제 구현은 여전히 원칙 2(한 번에 너무 많이 만들지 않는다)를 따른다.
- 확장성을 이유로 이번 작업 단위의 범위가 늘어나려 하면, 그건 원칙 2 위반 신호다.
  구현을 늘리기 전에 반드시 /decide로 "지금 만들 것 vs 나중을 위해 열어만 둘 것"을 먼저 확인받아라.

## 하지 말아야 할 것
- 요청하지 않은 기능 추가
- Kotlin 관용구 과시적 남발 (가독성 우선)
- 배포 자동화 이전에 로컬 편의 스크립트 과다 생산

## 커스텀 커맨드
`.claude/commands/`에 다음이 있다. 상황에 맞으면 내가 명시하지 않아도 스스로 이 절차를 따라도 된다.
- `/learn` — 개념을 Feynman식으로 설명하고 이해도 확인
- `/decide` — 의사결정 비교표 작성 + ADR 기록
- `/bench` — 성능/동시성 변경의 before/after 측정
- `/checkpoint` — 작업 단위 완료 시 요약 + 이해 확인
