# 의사결정 기록 (ADR)

이 문서는 아키텍처/기술적 의사결정을 시간순으로 기록한다.
`/decide` 커맨드가 새 결정을 이 문서 하단에 append한다.

형식:

```
## [YYYY-MM-DD] [결정 제목]
- 후보: A, B, C
- 선택: B
- 이유: ...
- 감수한 단점: ...
- 재검토 조건: (이 결정을 다시 봐야 할 상황이 있다면)
```

---

## [2026-09-14] Product/Inventory 재고 모델링 방식
- 후보: A. 개별 유닛(row) 모델, B. 수량 카운터 모델
- 선택: A. 개별 유닛 모델 — InventoryUnit이 각각 상태(AVAILABLE→RESERVED→SOLD/CANCELLED)를 가짐
- 이유: 한정 수량 판매 도메인에서 "어떤 유닛이 누구에게 선점되었는가"를 row 단위로 추적할 수 있어야
  선점/취소/재판매 흐름을 명확하게 설명 가능. 좌석 예매형 시나리오로 확장 시에도 모델이 자연스럽게 맞음.
  포트폴리오 목적상 동시성 제어(행 단위 락) 실습 대상으로도 적합.
- 감수한 단점: 재고 수량이 매우 큰 상품(수만 개 이상)에서는 row 수가 많아져 조회/집계 비용이
  수량 카운터 모델보다 큼. 이번 프로젝트 스케일에서는 문제되지 않는다고 판단.
- 재검토 조건: 한 상품의 재고가 수만 단위를 넘는 대량 상품 시나리오를 다루게 되면 카운터 모델 병행 검토.

## [2026-09-14] 상태 머신 표현 방식
- 후보: 1. Enum + 엔티티 내부 메서드별 검증, 2. 상태 전이 테이블(Map<Status, Set<Status>>)
- 선택: 2. 상태 전이 테이블
- 이유: 허용 전이를 한 곳에 선언적으로 모아두면 상태 다이어그램과 1:1로 매핑되어 면접 등에서
  설명하기 쉽고, 새 상태/전이 추가 시 변경 지점이 한 곳으로 좁혀짐.
- 감수한 단점: 매우 단순한 상태 머신 치고는 약간의 간접 계층이 추가됨(엔티티 메서드 하나로 끝날 일을
  별도 테이블 참조로 처리).
- 재검토 조건: 상태 전이마다 부수 로직(알림 발송 등)이 늘어나 State 패턴 수준의 분리가 필요해지면 재검토.

## [2026-09-15] InventoryUnit.reserve() 동시성 제어 락 전략
- 후보: A. 비관적 락(DB row lock, `SELECT ... FOR UPDATE`), B. 낙관적 락(`@Version`), C. Redis 분산락
- 선택: A. 비관적 락
- 이유: 한정 수량 상품의 "마지막 유닛을 여러 요청이 동시에 노리는" 상황은 hot-row 경합이
  전형적으로 발생하는 패턴이다. 낙관적 락은 경합이 심할수록 재시도가 폭증하는 구조라 이
  시나리오에서 가장 불리하다. 현재 배포는 단일 앱 인스턴스 전제(모듈러 모놀리식)이므로
  DB row lock만으로 정합성과 예측 가능한 처리량을 가장 단순하게 얻을 수 있고, 애플리케이션
  레벨 재시도 로직도 필요 없어 구현 복잡도가 가장 낮다.
- 감수한 단점: 경합이 심한 상품에서는 락을 잡은 트랜잭션이 끝날 때까지 다른 요청이 대기하므로
  응답 지연이 발생하고, 커넥션 풀 점유 시간이 늘어난다. 다중 WAS 인스턴스로 수평 확장하면
  DB row lock만으로는 부족해질 수 있다(Redis 분산락의 장점이 그때 비로소 실익이 됨).
- 재검토 조건: 다중 인스턴스 배포로 확장하거나, Waiting(대기열) 모듈과 락을 연계해야 하는
  시점에 Redis 분산락(C)으로 전환 검토. 그 전까지는 reserve 로직을 서비스 메서드 하나에
  캡슐화해 두어, 전환 시 변경 지점을 그 메서드로 한정한다(전략 패턴 등 사전 추상화는
  만들지 않음 — 2026-09-15 결정).

## [2026-09-15] confirm/cancel 요청 식별 방식
- 후보: A. `PurchaseAttempt.id`로 지정, B. `InventoryUnit.id` + `buyerId`로 지정, C. 별도
  예약 토큰(opaque token) 발급
- 선택: A. `PurchaseAttempt.id`
- 이유: reserve→confirm/cancel이 하나의 자원(attempt) 생명주기로 자연스럽게 이어지고,
  Purchase 모듈이 자기 리소스의 API 표면을 그대로 소유해 모듈 경계가 깔끔하게 유지된다.
  buyerId 값 비교만으로 소유권 검증도 부가 조건 없이 바로 처리할 수 있다. B는 유닛이
  CANCELLED→AVAILABLE로 재판매 사이클을 거치며 같은 unitId가 시간이 지나 다른 buyer의
  다른 시도를 가리킬 수 있어 "이 unitId+buyerId에 해당하는 최신 attempt" 조회라는 간접
  로직이 필요해진다. C(토큰 발급)는 이번 스코프(confirm/cancel API 하나)에 비해 과한
  인프라 추가로 판단했다.
- 감수한 단점: `attempt.id`가 순차 발급 Long이라 추측 가능하다. 더 근본적으로, 현재
  `buyerId`는 인증 시스템(User/Auth 모듈) 없이 요청 바디의 평문 문자열을 그대로 신뢰하고
  있어 "소유권 검증"이 실질적으로는 "값 일치 확인"에 가깝다 — 인증되지 않은 buyerId를
  스스로 주장하면 남의 attempt를 조작할 수 있는 구조적 한계가 있다(reserve()에도 이미
  있던 한계이며 이번 설계로 새로 생기는 문제는 아니다).
- 재검토 조건: User/Auth 모듈이 붙어 실제 인증이 도입되면, buyerId를 인증된 사용자
  식별자로 교체하고 소유권 검증 로직을 다시 점검한다.

## [2026-09-15] confirm/cancel 동시성 제어
- 후보: A. 비관적 락 재사용(reserve와 동일한 `SELECT ... FOR UPDATE`), B. 낙관적 락
  (`@Version`), C. 락 없이 상태 전이 검증(`transitionTo()`의 `IllegalStateException`)에만 의존
- 선택: B. 낙관적 락
- 이유: reserve()가 비관적 락을 택한 근거(여러 buyer가 같은 재고를 동시에 다투는 hot-row
  경합, 2026-09-15 위 결정)가 confirm/cancel에는 적용되지 않는다. 여기서는 이미 특정
  buyer가 선점한 유닛 하나를 다루므로, 문제가 될 수 있는 경합은 "같은 대상에 대한
  confirm/cancel이 드물게 동시에 들어오는 경우"뿐이다. 드문 충돌을 정확히 감지하는 것이
  목적이라는 점에서 낙관적 락의 교과서적 적용 조건에 더 가깝다고 판단했다. C는 같은
  트랜잭션 내 메모리 상태 기준 체크라 실제 동시 레이스는 막지 못해 제외했다.
- 감수한 단점: `InventoryUnit`(또는 `PurchaseAttempt`)에 버전 컬럼 추가가 필요하다. 충돌
  시 자동 재시도는 하지 않고 "현재 상태를 다시 확인하라"는 실패 응답으로 처리하기로
  했다. 이 선택은 벤치마크가 아니라 경합 패턴에 대한 논리적 추론에 근거한다 — 실제 충돌
  빈도나 지연 영향은 측정되지 않았다.
- 재검토 조건: 실제 운영에서 confirm/cancel 동시 충돌이 예상보다 잦아 재시도/실패 응답
  비용이 커지면 비관적 락(A)으로 재검토. 필요하면 `/bench`로 먼저 측정한다.

## [2026-09-15] cancel이 허용할 출발 상태
- 후보: A. RESERVED에서만 cancel 허용, B. RESERVED와 CONFIRMED 둘 다 cancel 허용
- 선택: A. RESERVED에서만
- 이유: `InventoryStatusTransitions`는 이미 CONFIRMED→CANCELLED도 허용해 두었지만(2026-09-14
  결정), CONFIRMED 취소는 사실상 환불 유스케이스이고 결제 모듈이 아직 없어 그 의미가
  불완전하다. 지금 API로 열면 "겉보기엔 되는데 실제 환불 로직은 없는" 어중간한 기능이
  생긴다. 결제/알림 없이도 명확하게 설명 가능한 범위로 한정했다(원칙 2).
- 감수한 단점: 도메인 엔티티(전이 규칙)가 허용하는 범위와 API가 노출하는 범위가 달라진다
  — CONFIRMED→CANCELLED 전이 자체는 막지 않되 이 유스케이스에서는 쓰지 않는다는 걸 코드
  주석으로 분명히 남겨야 나중에 헷갈리지 않는다.
- 재검토 조건: 결제/환불 플로우가 도입되어 CONFIRMED 취소(환불)가 실제 도메인
  유스케이스로 필요해지면, 별도 유스케이스/엔드포인트로 재검토.

## [2026-09-15] CI/테스트를 실제 MySQL로 검증하는 구조
- 후보: A. 이중 트랙(기존 H2 `test`는 유지하고 CI에서 `testMysql` 태스크를 추가로 실행),
  B. `test` 태스크 자체를 MySQL 기준으로 전환
- 선택: B
- 이유: 지금까지 모든 테스트가 H2(MODE=MySQL) 인메모리로만 돌아 실제 InnoDB 락 동작·
  방언 차이를 검증하지 못했다는 문제의식(`PurchaseServiceConcurrencyTest` 클래스 주석에
  이미 스스로 남긴 한계)에서 출발한 결정이다. "테스트를 통과했다"는 신호가 곧 실제 MySQL
  기준이 되어야 한다는 단일 진실 소스를 우선했다. 이중 트랙은 H2/MySQL 두 프로필을 계속
  동기화해야 하는 유지보수 비용과, 같은 테스트를 두 번 도는 CI 비용이 계속 누적된다.
- 감수한 단점: 로컬에서 `./gradlew test`를 돌리려면 MySQL이 떠 있어야 한다 — Docker
  없이 즉시 실행되던 이전 워크플로보다 로컬 온보딩 마찰이 커진다. 로컬 편의 스크립트를
  과다 생산하지 않는다는 원칙(CLAUDE.md)에 따라, 로컬 MySQL 구동은 최소한의 안내
  (docker-compose 등)만 제공하고 그 이상 자동화하지 않는다.
- 재검토 조건: 로컬 온보딩 마찰이 실제로 개발 속도를 저해한다고 판단되면 이중 트랙(A)으로
  회귀 검토.

## [2026-09-15] MySQL 테스트 프로필의 스키마 초기화 방식
- 후보: A. Hibernate `ddl-auto: create-drop`(H2와 동일 전략, datasource만 교체),
  B. Flyway/Liquibase 마이그레이션 도입 후 `ddl-auto: validate`, C. 정적 `schema.sql`
- 선택: B. Flyway
- 이유: 프로덕션 프로필(`application.yml`)은 이미 `ddl-auto: validate`로 스키마가
  별도로 존재한다고 가정하고 있는데, 정작 그 스키마를 만드는 마이그레이션 도구가
  프로젝트에 없었다(엔티티에서 파생되는 Hibernate 자동 생성에만 의존). 테스트 프로필만
  MySQL로 바꾸고 스키마 생성은 여전히 Hibernate 자동 생성(A)에 맡기면, "테스트가
  통과했다"는 신호가 프로덕션이 실제로 쓰는 스키마 관리 방식과는 여전히 무관하게 남는다.
  Flyway를 도입해 테스트에서 검증하는 스키마와 운영 배포 시 적용될 스키마 관리 방식을
  일치시키는 쪽을 선택했다.
- 감수한 단점: architect 서브에이전트는 이 결정이 "기존 테스트를 실제 MySQL 위에서
  통과시킨다"는 이번 세션 스코프를 넘어서는 신규 툴체인 도입(원칙 1/2/8 대상)이라고
  명시적으로 지적했다 — 마이그레이션 버전 관리라는 새 개념, 신규 의존성, 그리고 현재
  엔티티들을 역산해 baseline 마이그레이션 스크립트를 작성/유지해야 하는 부담이 생긴다.
  엔티티가 바뀔 때마다 대응하는 마이그레이션 스크립트도 함께 관리해야 한다. 이 단점을
  감수하고 B를 선택한 것은 사용자의 명시적 판단이다.
- 재검토 조건: 마이그레이션 스크립트 관리 부담이 엔티티 변경 빈도 대비 지나치게 커지면
  A(Hibernate 자동 생성)로 축소 검토.

## [2026-09-15] status 컬럼(InventoryUnit.status, PurchaseAttempt.status) 타입
- 후보: A. MySQL 네이티브 ENUM 유지(현재 `V1__baseline_schema.sql`이 채택한 형태, Hibernate가
  `@Enumerated(EnumType.STRING)`을 자동으로 이렇게 매핑함), B. `VARCHAR(20)` + Kotlin enum(검증
  책임을 애플리케이션 레벨로 위임)
- 선택: B. VARCHAR
- 이유: 이 프로젝트는 단일 애플리케이션만 이 DB에 쓰는 구조라, ENUM이 주는 DB 레벨 방어(다른
  소비자가 잘못된 문자열을 못 넣게 막는 것)의 실익이 낮다. 반대로 ENUM은 상태값을 추가할
  때마다 코드(Kotlin enum)와 DB(`ALTER TABLE ... MODIFY COLUMN`)를 이중으로 관리해야 하는데,
  이 동기화가 깨져도 `ddl-auto: validate`가 잡아주지 못한다는 걸 QA가 직접 실험으로 확인했다
  (ENUM 목록에서 값을 하나 빼도 39개 테스트 전부 통과 — 위 "MySQL 테스트 프로필의 스키마
  초기화 방식" 회귀 점검 참고). VARCHAR로 바꾸면 상태값 추가 시 스키마 변경 자체가
  필요없어져서 이 리스크가 구조적으로 사라진다.
- 감수한 단점: DB 레벨에서 잘못된 문자열이 저장되는 걸 막아주는 안전장치가 없어진다 — 검증
  책임이 전부 애플리케이션(`@Enumerated(EnumType.STRING)`)으로 넘어간다.
- 재검토 조건: 이 DB에 이 애플리케이션 외의 다른 쓰기 주체(별도 배치, 분리된 서비스 등)가
  생기면 ENUM이나 `CHECK` 제약 같은 DB 레벨 방어를 다시 검토한다.
- 구현 메모: 이번 세션에서는 기록만 하고 실제 마이그레이션(V2, `MODIFY COLUMN ... VARCHAR(20)`)은
  적용하지 않았다 — 원칙 2(범위 과다 금지)에 따라 다음 세션에서 별도 작업 단위로 진행한다.
- 적용 완료(2026-09-15): `V2__status_columns_to_varchar.sql`로 `inventory_unit.status`,
  `purchase_attempt.status`를 `VARCHAR(20) NOT NULL`로 전환. 값 자체(문자열)는 바뀌지 않아
  데이터 마이그레이션은 불필요했다. Kotlin 엔티티는 이미 `@Enumerated(EnumType.STRING)` +
  `length = 20`으로 선언돼 있어 코드 변경 없이 스키마만 바꾸면 됐다. 로컬에 MySQL 8을 직접
  설치해 실제 구동 위에서 검증했고(`flyway_schema_history`에 V2 적용 확인, `SHOW COLUMNS`로
  `varchar(20)` 확인), 기존 39개 테스트 전부 통과(실패/에러 0건).

## [2026-09-15] MySQL 고유 락(gap lock) 겨냥 신규 동시성 테스트 — 시나리오/방법론
- 후보(시나리오): A1. 같은 product_id 내 서로 다른 AVAILABLE 유닛 동시 reserve, A2. status가
  섞여 있을 때(AVAILABLE 1개 + 다른 상태 다수) non-AVAILABLE row까지 잠기는가, A3. 서로 다른
  product_id의 reserve끼리 블로킹되는가, A4. reserve 트랜잭션이 열려있는 동안 같은
  product_id로 신규 InventoryUnit INSERT(재입고)가 블로킹되는가
- 후보(방법론): B1. 기존 `PurchaseServiceConcurrencyTest`와 동일한 래치/타이밍 패턴, B2.
  `performance_schema.data_locks` / `information_schema.innodb_trx` 직접 조회, B3. 하이브리드
  (래치로 블로킹 신호를 먼저 확인하고 그 순간 data_locks/innodb_trx를 1회 스냅샷 조회)
- 선택: A4 + B3
- 이유: A1은 `findAvailableForUpdate`가 `ORDER BY id ASC LIMIT 1`이라 실제로는 항상 같은
  row를 다퉈 신규성이 낮고 기존 `PurchaseServiceConcurrencyTest`가 사실상 이미 커버한다.
  A4는 gap lock을 가장 교과서적으로 시연하고("재고 없음" 판정과 재입고가 겹치는 실제 운영
  시나리오이기도 함), `inventory_unit.status`에 인덱스가 없어 `product_id` 인덱스로 스캔 후
  status로 필터링하는 현재 쿼리 구조의 락 범위를 직접 드러낸다. 방법론은 B1 단독으로는
  "막혔다"만 보여줄 뿐 gap lock임을 증명하지 못하고, B2 단독은 raw JDBC 별도 커넥션이
  필요해 스코프 대비 구현 비용이 크다. B3(하이브리드)가 증명력과 구현 비용의 절충점이라고
  판단했다.
- 감수한 단점: A2(non-AVAILABLE row까지 잠기는지 — status 미인덱스 설계의 대가를 더 직접
  드러내는 시나리오)와 A3는 이번 세션에서 다루지 않는다(원칙 2, 세 번째 세션째 미뤄진 세
  항목 중 이번엔 이 하나만 착수하기로 사용자가 결정). B3는 B2 단독 대비 완전한 성공을
  보장하지 않는다 — 스냅샷 조회 시점이 늦으면 락이 이미 해제됐을 수 있어 최소한의
  재시도/폴링이 필요하고, B1보다는 flaky 위험이 높다.
- 재검토 조건: A2가 필요해지면 이 ADR과 같은 절차로 별도 세션에서 재검토한다. B3가 실제
  구현/CI에서 반복적으로 flaky하면 B1(신호만)로 축소하거나 B2(스냅샷 중심)로 강화하는 것을
  재검토한다.
- 구현 메모(2026-09-15): `PurchaseServiceGapLockConcurrencyTest`로 구현하는 과정에서 A4의
  전제 조건을 실측으로 정정했다. 처음 예상과 달리, AVAILABLE 유닛이 1개 남아 있는 상태에서는
  재입고 INSERT가 블로킹되지 않았다(`ORDER BY id ASC LIMIT 1`이 조건을 만족하는 첫 행에서
  스캔을 멈추므로, next-key lock이 그 행 이전 갭만 잠그고 이후 구간은 잠그지 않음). AVAILABLE
  유닛이 0개(재고 소진)일 때만 "조건을 만족하는 행 없음"을 확정하려고 구간 끝(supremum)까지
  스캔하며 gap lock이 걸려 재입고 INSERT가 블로킹됨을 직접 확인했다(`LOCK_MODE =
  X,INSERT_INTENTION`/`WAITING`, `LOCK_DATA = supremum pseudo-record`). 그래서 최종 테스트는
  "재고 1개를 만들고 즉시 선점해 소진시킨 뒤" 재입고를 시도하는 구조다 — A4가 가리키던 "재고
  없음 판정과 재입고가 겹치는 실제 운영 시나리오"와 정확히 일치하는 케이스라 시나리오 선택
  자체는 바뀌지 않았고, 어떤 재고 상태에서 시연되는지만 실측으로 구체화됐다. 상세 근거는
  테스트 클래스 KDoc 참고. 로컬 MySQL 8.0.46에서 신규 테스트 6회 연속 통과, 전체 40개(기존
  39 + 신규 1) 테스트 통과 확인. CI(`twshop` 계정)에는 이 테스트가 필요로 하는
  `PROCESS`/`performance_schema` 조회 권한이 기본적으로 없어 `.github/workflows/ci.yml`에
  root로 부여하는 스텝을 추가했다(스키마가 아니라 계정 권한이라 Flyway 대상 아님).
- 구현 메모(2026-09-16, A2): 같은 클래스에 두 번째 `@Test`로 A2(non-AVAILABLE row 잠금 여부)를
  추가했다. 유닛 3개(id 오름차순)를 만들고 앞 두 개를 reserve()로 RESERVED로 바꿔 세 번째만
  AVAILABLE로 남긴 뒤, `findAvailableForUpdate`가 그 세 번째 row를 찾아 잠근 채 커밋을
  보류하는 동안, 별도 트랜잭션이 raw JDBC로 *첫 번째(non-AVAILABLE, 매칭되지 않은)* row를
  `UPDATE ... WHERE id = ?`로 직접 건드려봤다. 실제로 블로킹됐고,
  `performance_schema.data_locks`에서 `LOCK_TYPE=RECORD`, `LOCK_STATUS=WAITING`,
  `LOCK_DATA=<그 row의 PK>`가 관측됐다 — status가 인덱스에 없어 `product_id` 인덱스로
  스캔하며 examine한 non-matching row까지 InnoDB가 잠근다는 ADR의 예상을 실측으로 확인했다.
  A4가 증명한 gap(다음 레코드 없음 구간) lock과 달리, A2는 이미 존재하는 특정 row에 대한
  일반 record lock이라는 점이 다르다. 로컬 MySQL 8.0에서 신규 테스트 5회 연속 통과, 전체
  41개(기존 40 + 신규 1) 테스트 통과 확인. A3(서로 다른 product_id 간 비블로킹)는 이번
  세션에서 다루지 않는다(원칙 2 — 이번 세션 시작 시 A2/A3 중 A2만 진행하기로 결정함).
- 구현 메모(2026-09-16, A3): 같은 클래스에 세 번째 `@Test`로 A3(서로 다른 product_id 간
  비블로킹)를 추가했다. A2/A4가 "블로킹됐다"를 증명하는 구조였던 것과 달리 A3는 정반대
  방향(안 막혔다)을 증명해야 해서 별도 검증 구조를 새로 만들었다 — 기존 `LockSnapshot`/
  `pollForLockSnapshot`은 "대기 중인 락의 증거"를 찾는 도구라 대기 중인 락이 아예 없어야
  하는 이 시나리오에는 맞지 않았다. 상품 X, Y(각 재고 1개)를 만들고, 스레드1이 X의 유닛을
  `FOR UPDATE`로 잠근 채 커밋을 보류한 동안, 스레드2가 별도 트랜잭션으로 Y의 유닛을
  조회하게 했다. 증명은 세 겹으로 쌓았다: (1) Y 조회가 짧은 타임아웃(500ms) 안에 끝남,
  (2) 그 시점에 X는 아직 커밋을 "시도조차" 하지 않았음을 상태로 확정(래치를 메인 스레드가
  쥐고 있어 레이스 컨디션 없이 보장됨), (3) 그런데도 X를 의도적으로 2초 더 붙잡아 둔 뒤,
  "Y의 조회 완료 시각 < X의 커밋 시도 시각"이라는 등호 없는 부등식을 확인함(A2/A4의
  `>=`와 정반대 방향). 마지막으로 Y가 실제로 자기 작업(Y 소속 유닛 잠금)을 정상 완료했음도
  `found.size == 1`과 소속 product 검증으로 확인했다. 로컬 MySQL 8.0에서 신규 테스트 5회
  연속 통과, 전체 42개(기존 41 + 신규 1) 테스트 통과 확인. 이것으로 2026-09-15 ADR이 남긴
  A2/A3 두 항목이 모두 마무리됐다. 프로덕션 코드(`InventoryUnitRepository`,
  `PurchaseService` 등)는 이번 작업에서도 변경하지 않았다.
- 후속 수정(2026-09-16, A3 flaky 리스크 제거): qa 검증에서 위 (1) `foundYLatch.await(500,
  TimeUnit.MILLISECONDS)`가 "CI에서 실측 안 된 이론적 flaky 리스크"로 지적됐다. 재검토해보니
  이 테스트의 실제 증명력은 (2)의 `assertEquals(-1L, commitInitiatedAtNanos.get(), ...)`(래치를
  메인 스레드가 쥐고 있어 레이스 없이 100% 보장)와 (3)의 `foundYAtNanos < commitInitiatedAtNanos`
  부등식, 이 두 결정론적 assertion만으로 이미 완결된다 — (1)은 있어도 없어도 결론에 기여하지
  않는 진단용 하한 체크였다. 그런데 500ms라는 타이트한 값 때문에 느린 CI 러너(GC 일시정지,
  커넥션 풀 경합 등)에서 실제로는 안 막혔는데도 우연히 타임아웃을 넘겨 테스트가 false
  failure를 낼 이론적 여지가 있었다. 원칙: **타이밍 assertion이 증명에 기여하지 않으면
  널널하게 풀고, 결정론적 체크만 증명을 전담하게 한다.** 이 파일의 다른 래치들과 같은 10초로
  늘렸다 — X를 붙잡는 2초보다 충분히 길어 정상 케이스에서는 항상 통과하고, 회귀 탐지력도
  줄지 않는다: 만약 누군가 실수로 product_id 필터를 없애 진짜 블로킹이 재발하면 Y는 X의
  커밋(2초 후)까지 기다렸다가 끝나므로 이 await는 여전히 true를 반환하지만, 그 시점엔 이미
  `commitInitiatedAtNanos`가 -1이 아니게 되어 바로 다음 줄의 `assertEquals`가 실패로 잡아낸다
  (테스트 코드 자체에 이 논리를 주석으로 남김). 로컬 MySQL 8.0에서 A3 단독 5회 연속 통과,
  전체 42개 테스트 회귀 없음 확인. 프로덕션 코드는 변경하지 않았다.

## [2026-09-16] PurchaseReserveBenchmark를 autocannon 기반 실부하테스트로 전환
- 후보(관계): A. 완전 대체, B. 병행 유지(기존은 순수 락 경합, 신규는 HTTP 스택 전체 처리량 —
  서로 다른 질문에 답함)
- 후보(앱 구동): A. 로컬 수동 구동(`./gradlew bootRun` 띄운 뒤 실행), B. 서버 기동→헬스체크
  대기→부하테스트→종료까지 자동화한 스크립트
- 후보(CI 포함): A. CI(GitHub Actions) 통합, B. 로컬 전용, 결과는 `docs/benchmarks/`에 수동 기록
- 선택: 병행 유지 + 로컬 수동 구동 + 로컬 전용
- 이유: 기존 `PurchaseReserveBenchmark`는 서비스 메서드를 직접 호출하는 인메모리 벤치마크라
  HTTP 계층(Tomcat 스레드풀, 커넥션 수립, 직렬화)을 거치지 않는다 — "락 전략이 처리량에
  미치는 영향"과 "실제 서비스가 이 트래픽을 견디는가"는 다른 질문이라 완전 대체 대신 병행
  유지를 택했다. 서버 기동/종료 자동화는 그 자체로 별도 인프라 자산(헬스체크 대기, 프로세스
  정리 등)이라 이번 세션 스코프를 넘는다고 판단해 로컬 수동 구동으로 시작한다. CI 통합은
  공유 러너의 성능 노이즈로 TPS/지연 수치가 실행마다 들쭉날쭉해 회귀 판정 기준으로 쓰기
  부적합하고, 기존 인메모리 벤치마크도 CI 기본 실행에서 제외된 전례와 일관되게 로컬 전용으로
  뒀다("측정 없이 성능이 개선됐다고 말하지 않는다"는 원칙에 오히려 더 부합).
- 감수한 단점: 실행 재현성이 사람의 수동 절차(MySQL/앱 기동 순서 등)에 의존하고, 지속적인
  성능 회귀 감시는 하지 못한다.
- 재검토 조건: 서버 기동 자동화가 반복적으로 필요해지거나(수동 절차의 마찰이 실제로 느껴지면),
  CI 성능 회귀 감시가 프로젝트 목표로 명시되면 재검토. 사용자가 "나중에 자동화 확장성은
  고려해두자"고 명시했으므로, 구현 시 스크립트/설정을 자동화 도구가 그대로 재사용할 수 있는
  독립 실행 가능한 형태(예: CLI 인자만 받는 Node 스크립트)로 만들어 둔다 — 자동화 자체는
  만들지 않되 나중에 감싸기 쉬운 모양으로만 설계한다(CLAUDE.md 원칙 8).

## [2026-09-16] 테스트 피라미드 공식화 범위
- 후보: A. 문서화만(`docs/testing-strategy.md` 신규 작성, 코드/설정 변경 없음), B. 문서화 +
  `@Tag` 분류(8개 테스트 클래스에 `unit`/`integration`/`concurrency` 태그 부여, `benchmark`
  태그는 기존 유지), C. B + Gradle 태스크 분리(`unitTest`/`integrationTest` 등 별도 태스크,
  CI 워크플로 단계 분리)
- 선택: B
- 이유: architect가 8개 테스트 파일을 실제로 조사해보니 순수 단위 테스트는
  `InventoryUnitTest` 1개뿐이고 나머지 7개는 전부 `@SpringBootTest`로 실 MySQL을 띄우는
  통합/동시성 테스트였다 — 이 프로젝트의 핵심 학습 대상(비관적/낙관적 락, gap lock)이
  애초에 DB 트랜잭션 경계 없이는 검증 불가능하기 때문이다. 이 구조에서 C(Gradle 태스크
  분리)는 "빠른 단위 테스트만 따로 돌린다"는 본래 취지를 살리지 못한다 — 단위 테스트가
  1개 파일(7케이스)뿐이라 별도 태스크로 얻는 실행 시간 이득이 사실상 없는데, CI 워크플로
  변경과 로컬 온보딩 문서라는 새 표면적만 늘어난다(원칙 8 "로컬 편의 스크립트 과다 생산
  금지"에 저촉). A(문서화만)는 안전하지만 분류가 코드에 강제되지 않아 문서-코드
  drift 위험이 있고 "공식화"라는 취지에 비해 약하다. B는 `@Tag`가 이미 `benchmark`로
  선례가 있는 JUnit5 표준 기능이라 새 개념 도입이 아니면서도(원칙 1 절차를 생략할 수 있는
  반복 작업에 가까움), 문서와 코드 사이 최소한의 연결고리를 만들어 A보다 "공식화"에
  가깝다.
- 감수한 단점: 지금 이 태그를 실제로 소비하는 Gradle 태스크나 CI 단계가 없어 "선언만
  하고 아무도 안 쓰는" 메타데이터 상태로 남는다. 태그를 붙였다는 사실만으로 실행 시간이나
  피드백 속도가 개선되지는 않는다.
- 재검토 조건: 순수 unit 테스트가 유의미하게 늘어나 별도 Gradle 태스크로 실행 시간
  이득이 실제로 생기면 C(태스크 분리)를 재검토한다.

## [2026-09-16] Waiting 모듈 대기열 설계 (저장소/순번 발급/재진입 정책)
- 결정 배경: 이 결정은 별도 세션(architect)에서 사용자와 후보 비교를 마친 뒤 확정되어
  넘어온 것을 기록한다. 이 세션에서 새로 후보를 비교하지 않았다.
- 후보(저장소/자료구조): A. DB 테이블 + 폴링(순번을 컬럼으로 저장, 주기적으로 순위 재계산),
  B. Redis List(FIFO 큐, LPUSH/RPOP), C. Redis Sorted Set(ZADD/ZRANK)
- 후보(순번 발급 방식): A. 애플리케이션 서버의 로컬 타임스탬프, B. DB auto-increment 시퀀스,
  C. productId별 Redis 시퀀스 카운터(INCR)를 ZADD의 score로 사용
- 후보(재진입 정책): A. 멱등 — 이미 대기열에 있으면 기존 순번 유지(재요청 무시), B. 비멱등 —
  재진입 시 항상 새 순번 발급(ZADD가 같은 member의 score를 덮어쓰므로 자동 구현됨)
- 선택: C(Redis Sorted Set) + C(Redis INCR 시퀀스) + B(비멱등, 항상 새 순번)
- 이유:
  - 저장소: 대기열은 "순위를 빠르게 재계산해서 보여준다"가 핵심 연산인데, Sorted Set은
    ZADD(진입)와 ZRANK(순위 조회)가 각각 하나의 원자적 커맨드로 이 두 연산에 정확히
    대응한다. List(B)는 순서는 보장하지만 "특정 buyerId의 현재 순위"를 얻으려면 전체를
    스캔해야 해서 순위 조회라는 이번 스코프의 핵심 API에 맞지 않는다. DB 폴링(A)은 매
    조회마다 순위를 다시 계산하는 쿼리 비용과 갱신 지연이 함께 발생하고, 무엇보다 이
    프로젝트가 지금까지 Redis를 한 번도 실사용하지 않았다는 점에서 이번 단위가 그 진입점
    으로 적합하다고 판단했다(포트폴리오 목적상 학습 밀도 측면에서도).
  - 순번 발급: Redis INCR은 Redis의 단일 스레드 커맨드 실행 모델 덕에 그 자체로 원자적이라
    여러 클라이언트가 동시에 호출해도 서로 다른 값을 정확히 하나씩 나눠 갖는다. 이 값을
    그대로 ZADD의 score로 쓰면 "동시에 여러 명이 진입해도 순번이 절대 겹치지 않는다"는
    보장이 애플리케이션 코드 없이 Redis 커맨드 두 개(INCR, ZADD)의 조합만으로 성립한다.
    로컬 타임스탬프(A)는 서버 인스턴스가 여러 대면 시계 오차로 순서가 뒤집힐 수 있고, DB
    시퀀스(B)는 이미 새로 도입하는 Redis 대신 굳이 다른 저장소를 하나 더 걸치는 구조라
    저장소 선택 결정과 어긋난다.
  - 재진입 정책: "줄을 서 있다"는 상태를 안정적인 신원 확인이 아니라 "연결/재요청이 유지되고
    있다"는 신호로 재정의하는 실무적 타협이다. 이 프로젝트는 User/Auth 모듈이 아직 없어
    buyerId를 요청 바디의 평문 문자열로만 신뢰한다(2026-09-15 confirm/cancel ADR과 동일한
    한계). 그 상태에서 "이 buyer가 실제로 이탈했는가"를 판별하는 것은 안정적 신원 확인 없이는
    풀 수 없는 문제이므로, 대신 "재진입하면 새 순번"이라는 단순한 대리 규칙으로 치환했다.
    이렇게 하면 순번이 유지될 때(멱등, A) 발생하는 "매크로/무분별한 재요청이 손해가 없다"는
    문제가, 재요청마다 실제 비용(순번 밀림)이 발생하는 구조로 바뀌어 최소한의 억제 효과를
    갖는다. 구현도 더 단순하다 — "이미 대기열에 있는지" 확인하는 조건 분기 없이 매번
    INCR + ZADD만 수행하면 되고, ZADD가 같은 member의 score를 덮어쓰는 동작을 그대로
    활용할 뿐 별도 로직이 필요 없다.
- 감수한 단점:
  - 재진입 비멱등 정책은 실수로(네트워크 재시도, 중복 클릭 등) 다시 진입한 정상 사용자에게도
    똑같이 순번 손해를 입힌다 — "악의적 재요청만 억제하고 싶다"는 의도를 정확히 구현하지
    못하고, 의도와 무관하게 재진입 자체에 비용을 매기는 거친 대리 규칙이다.
  - Redis는 이 프로젝트에서 지금까지 한 번도 쓰지 않은 새 인프라 의존성이다 — 별도 실행
    프로세스(로컬 Docker, 배포 환경 프로비저닝)와 장애 시나리오(Redis 다운 시 대기열 기능
    전체 불가)가 새로 생긴다.
  - INCR과 ZADD는 두 개의 별도 커맨드라 그 사이에 프로세스가 죽으면 시퀀스 값만 소진되고
    ZADD가 되지 않는 경우가 생길 수 있다. 이는 순번에 "결번"을 남길 뿐 순번 중복이나
    순서 역전을 일으키지 않으므로 이번 스코프(진입/순위 조회)에서는 정합성 문제가 아니라고
    판단했다 — 결번 자체를 없애려면 Lua 스크립트로 INCR+ZADD를 하나의 원자적 단위로
    묶어야 하는데, 지금은 그 비용을 들일 필요가 없다고 봤다.
- 재검토 조건:
  - 순번 도달 시 reserve 허용을 연계하는 다음 단위에서, InventoryUnit.reserve()의 락 전략
    (2026-09-15 결정, 현재 비관적 락)과 Redis 대기열을 함께 다뤄야 하는 시점에 Redis
    분산락 전환 여부를 재검토한다(2026-09-15 ADR의 재검토 조건과 동일한 트리거).
  - 이탈/포기 처리(TTL, leave API)를 도입하는 시점에 "재진입=새 순번" 대리 규칙이 여전히
    타당한지, 아니면 명시적 이탈 신호와 병행해야 하는지 재검토한다.
  - 대기열 크기 제한이 필요해지면(마케팅상 "선착순 N명" 등) 이번 설계에 별도 개념(최대
    크기, 마감 이후 진입 거부)을 추가로 검토한다.
  - User/Auth 모듈이 도입되어 buyerId가 인증된 식별자로 바뀌면, "재진입=새 순번" 대신
    실제 이탈 감지(세션/커넥션 기반)로 대체할 수 있는지 재검토한다.
  - 실제 재진입 남용(매크로 등) 빈도가 무시할 만한 수준이거나, 반대로 정상 사용자의 실수
    재진입으로 인한 불만이 크면 정책 자체를 재검토한다.
- 구현 메모(2026-09-16): 위 결정을 그대로 구현했다 — `waiting.domain.WaitingRank`(0-based→
  1-based 변환 값 객체), `waiting.infrastructure.WaitingQueueRepository`(`StringRedisTemplate`
  래핑, INCR+ZADD/ZRANK), `waiting.application.WaitingService`(enter/getRank, productId 존재
  검증은 `ProductService.getProduct()` 재사용), `waiting.api`(컨트롤러/DTO/예외 핸들러,
  `PurchaseController`/`PurchaseExceptionHandler` 패턴 그대로 모방)까지 4계층을 채웠다.
  JPA 엔티티는 만들지 않았다(ADR대로 Redis가 source of truth). `build.gradle.kts`에
  `spring-boot-starter-data-redis` 추가, `application.yml`(main/test)에
  `spring.data.redis.host/port` 환경변수 오버라이드 추가, `docker-compose.yml`에 `redis:7-alpine`
  서비스 추가. ADR에 없던 추가 변경으로 `.github/workflows/ci.yml`에도 동일한 Redis 서비스
  컨테이너를 추가했다 — 새 인프라 의존성이 생겼으니 CI에서도 Redis 없이는 신규 테스트가
  실패하기 때문이다(기존 MySQL 서비스 컨테이너 패턴을 그대로 따름). 테스트는 계층별로
  분리했다: `WaitingServiceTest`(integration, 5케이스 — 진입/순번 발급, 순서, 없는 buyerId
  404, 없는 productId 404, **재진입 시 새 순번 발급 + ZCARD로 이전 순번이 남지 않음을
  직접 확인**), `WaitingControllerTest`(integration, 6케이스 — HTTP 계약), `WaitingService
  ConcurrencyTest`(concurrency, 1케이스 — 50개 스레드 동시 진입 시 발급 시퀀스가 1..50까지
  결번/중복 없이 정확히 한 번씩만 나오는지 확인, INCR의 원자성에 근거). `docs/testing-strategy.md`
  매핑 표에도 3개 클래스를 추가했다. 로컬에서 `docker run -d --name twshop-redis -p
  6379:6379 redis:7-alpine`로 띄우고 `./gradlew test --rerun-tasks` 실행 결과 기존 42개 +
  신규 12개 = 54개 전체 통과, `WaitingServiceConcurrencyTest` 단독 5회 연속 통과 확인.
  순번 도달 시 reserve 연계, 이탈/TTL 처리, 대기열 크기 제한은 이번 단위에서 다루지 않았다
  (ADR의 재검토 조건에 명시된 대로 다음 단위로 남겨둠). 기존 `product`/`purchase` 모듈
  프로덕션 코드는 변경하지 않았다.
