spring:
  application:
    name: twshop
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:twshop}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USERNAME:twshop}
    password: ${DB_PASSWORD:twshop}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
